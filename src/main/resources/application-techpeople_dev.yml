spring:
  application:
    name: techpeople
  datasource:
    url: jdbc:h2:mem:testdb
    driverClassName: org.h2.Driver
    username: sa
    password: password
  h2:
    console:
      enabled: true
      
app:
  security:
    jwt-signing-key: 123dsdfdf23123

logging:
  level:
    org.hibernate: DEBUG